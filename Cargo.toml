[package]
name = "float_lambda"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
rand = "0.8"
phf = { version = "0.12", features = ["macros"] }
paste = "1.0.15"

[features]
ml = []

    